#include <iostream>
using namespace std;

int main() {
    int num;
    cout<<"enter a number of rows:";
    cin>>num;
    if(num>0){
    for(int i=0; i<num; i++) {

        for(int j=0; j<=i; j++) {
              if(j==8){
                cout<<'J';
            continue;}
            cout << char('A' + j);

        }
